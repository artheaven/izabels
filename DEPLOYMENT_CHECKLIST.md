# Чеклист готовности к деплою

## 🔍 Статус проверки

### ✅ Backend (Railway) - Готово с замечаниями

**Что работает:**
- ✅ Package.json правильно настроен
- ✅ Build скрипт (`tsc`) есть
- ✅ Start скрипт (`node dist/server.js`) есть
- ✅ Prisma schema настроен
- ✅ Миграции созданы
- ✅ TypeScript конфигурация правильная
- ✅ CORS настроен (читает из env)
- ✅ PORT динамический (process.env.PORT)
- ✅ Middleware настроены
- ✅ Роуты организованы

**⚠️ Требует внимания:**

1. **❌ КРИТИЧНО: Uploads будут теряться**
   - Railway использует ephemeral filesystem
   - После каждого деплоя/рестарта uploads пропадут
   - **Решение:** Интегрировать Cloudinary или AWS S3

2. **❌ Отсутствует postinstall скрипт**
   - Prisma generate должен запускаться автоматически
   - **Нужно добавить:** `"postinstall": "prisma generate"`

3. **❌ Отсутствует FRONTEND_URL в .env**
   - CORS не знает production домен
   - **Нужно добавить:** `FRONTEND_URL=https://your-vercel-domain.vercel.app`

4. **⚠️ Server слушает только PORT**
   - Railway может требовать 0.0.0.0
   - **Рекомендуется:** `app.listen(PORT, '0.0.0.0')`

5. **❌ Отсутствует .env.example**
   - Нет примера для деплоя
   - **Нужно создать**

### ✅ Frontend (Vercel) - Готово с замечаниями

**Что работает:**
- ✅ Package.json правильно настроен
- ✅ Build скрипт (`next build`) есть
- ✅ Start скрипт (`next start`) есть
- ✅ Next.js конфигурация есть
- ✅ API URL читается из env
- ✅ TypeScript конфигурация правильная
- ✅ Tailwind настроен

**⚠️ Требует внимания:**

1. **❌ next.config.js - только localhost**
   - Images не будут загружаться с Railway
   - **Нужно добавить:** Railway домен в remotePatterns

2. **❌ Отсутствует .env.example**
   - Нет примера для деплоя
   - **Нужно создать**

3. **⚠️ API URL хардкод в .env.local**
   - Нужно будет обновить после деплоя backend
   - **Должно быть:** `NEXT_PUBLIC_API_URL=https://your-railway-domain.railway.app`

### 🔗 Связи между сервисами

**Backend ➡️ Database:**
- ✅ DATABASE_URL настроен через env
- ✅ Prisma schema правильный
- ✅ Миграции готовы
- ⚠️ **Railway:** Нужно создать PostgreSQL service и скопировать DATABASE_URL

**Frontend ➡️ Backend:**
- ✅ API URL читается из NEXT_PUBLIC_API_URL
- ✅ Axios настроен с baseURL
- ⚠️ **Vercel:** Нужно добавить env переменную NEXT_PUBLIC_API_URL

**Backend ➡️ Frontend:**
- ✅ CORS настроен для чтения FRONTEND_URL
- ⚠️ **Railway:** Нужно добавить env переменную FRONTEND_URL

## 🚨 Критические проблемы

### 1. Uploads/Images хранение
**Проблема:** Railway использует ephemeral filesystem, все файлы в `/uploads` будут теряться после рестарта

**Решения:**
- **Option A (Рекомендуется):** Cloudinary
  - Бесплатный tier: 25GB
  - CDN включен
  - Автоматическая оптимизация
  - Легкая интеграция с Node.js
  
- **Option B:** AWS S3
  - Более дешево для больших объемов
  - Требует больше настройки
  
- **Option C:** Railway Volumes
  - Persistent storage
  - Дороже (~$0.25/GB/month)

### 2. Environment Variables отсутствуют
Нужно создать переменные окружения в:
- **Railway:** DATABASE_URL, JWT_SECRET, FRONTEND_URL, PORT (автоматически), NODE_ENV=production
- **Vercel:** NEXT_PUBLIC_API_URL

## 📋 Пошаговый план деплоя

### Этап 1: Подготовка Backend
1. ✅ Добавить postinstall скрипт в package.json
2. ✅ Создать .env.example
3. ✅ Обновить server.ts для 0.0.0.0
4. ⚠️ Решить проблему с uploads (интегрировать Cloudinary)

### Этап 2: Подготовка Frontend
1. ✅ Обновить next.config.js с Railway domain
2. ✅ Создать .env.example

### Этап 3: Deploy Backend на Railway
1. Создать новый проект на Railway
2. Подключить PostgreSQL database
3. Скопировать DATABASE_URL
4. Добавить переменные окружения
5. Подключить GitHub репозиторий
6. Deploy

### Этап 4: Deploy Frontend на Vercel
1. Создать новый проект на Vercel
2. Подключить GitHub репозиторий
3. Добавить NEXT_PUBLIC_API_URL (из Railway)
4. Deploy

### Этап 5: Настройка
1. Обновить FRONTEND_URL на Railway (Vercel URL)
2. Redeploy backend
3. Запустить миграции Prisma на Railway
4. Запустить seed (создать админа)
5. Проверить все функции

## 🎯 Оценка готовности

- **Backend:** 70% готов (нужна интеграция для файлов)
- **Frontend:** 90% готов (нужны минорные правки)
- **Infrastructure:** 50% готов (нужны env переменные и настройка)

## 📝 Следующие шаги

1. **Срочно:** Интегрировать Cloudinary для uploads
2. **Срочно:** Добавить postinstall скрипт
3. **Важно:** Создать .env.example файлы
4. **Важно:** Обновить next.config.js
5. **Можно позже:** Оптимизация production настроек

---

**Дата проверки:** 26.11.2025
**Версия:** 1.0.0-MVP

