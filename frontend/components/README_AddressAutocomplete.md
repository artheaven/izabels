# 📍 AddressAutocomplete - Компонент автозаполнения адресов

## Описание
Компонент для автозаполнения адресов с использованием **Nominatim API** (OpenStreetMap).
Поддерживает поиск улиц в Варне с умным автодополнением.

## Технологии
- **Nominatim API** (бесплатный, на базе OpenStreetMap)
- **React + TypeScript**
- Дебаунсинг запросов (800мс для соблюдения лимитов)
- Навигация клавиатурой (↑↓ Enter Esc)

## Возможности
✅ Поиск улиц в пределах Варны  
✅ Автодополнение при вводе (минимум 2 символа)  
✅ Дебаунсинг 800мс для оптимизации запросов  
✅ Навигация клавиатурой  
✅ Красивый UI с иконками и лоадером  
✅ Поддержка болгарского языка  
✅ Ограничение поиска по Варне, Болгария  

## Использование

```tsx
import AddressAutocomplete from '@/components/AddressAutocomplete';

<AddressAutocomplete
  value={address}
  onChange={(value) => setAddress(value)}
  placeholder="Начнете да пишете улица..."
  required={true}
  city="Varna"
/>
```

## Props

| Prop | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| `value` | `string` | - | Текущее значение адреса |
| `onChange` | `(value: string) => void` | - | Callback при изменении |
| `placeholder` | `string` | "Начнете да пишете адрес..." | Placeholder |
| `required` | `boolean` | `false` | Обязательное поле |
| `city` | `string` | "Varna" | Ограничение по городу |
| `disabled` | `boolean` | `false` | Отключить поле |

## API
- **Endpoint**: `https://nominatim.openstreetmap.org/search`
- **Лимиты**: 1 запрос в секунду (соблюдается через дебаунсинг 800мс)
- **API ключ**: Не требуется ✅
- **User-Agent**: Обязателен (IzabelsFlowerShop/1.0)
- **Стоимость**: Бесплатно ✅

## Координаты Варны
- Широта: `43.2141`
- Долгота: `27.9147`
- Радиус поиска: ~15км от центра

## Пример ответа API

```json
{
  "features": [
    {
      "properties": {
        "name": "улица Цар Симеон I",
        "street": "улица Цар Симеон I",
        "housenumber": "23",
        "city": "Varna",
        "postcode": "9000"
      },
      "geometry": {
        "coordinates": [27.9147, 43.2141]
      }
    }
  ]
}
```

## Интеграция в форму заказа
Компонент интегрирован в `/app/koshnica/page.tsx`:
- Используется для доставки **во Варне**
- Для доставки по всей Болгарии используется обычный input (нужно указать город)

## Альтернативы
Если Photon API перестанет работать, можно использовать:
1. **Nominatim** (OpenStreetMap) - требует User-Agent, 1 запрос/сек
2. **Google Places API** - платный, самый точный
3. **Собственная база данных** улиц Варны

## Производительность
- ⚡ Быстрый отклик благодаря дебаунсингу
- 📦 Компактный размер (нет внешних зависимостей)
- 🔥 Оптимизирован для мобильных устройств

## Лицензия
Photon API: [Open Database License (ODbL)](https://www.openstreetmap.org/copyright)

